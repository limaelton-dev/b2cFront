## Inicialização do projeto

Faça um arquivo `.env.local` e coloque as informações indicadas no `.env.local.example`. Após isso, execute o comando no bash para executar o programa para fins de desenvolvimento:

```bash
npm run dev
```
